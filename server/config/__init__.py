from __future__ import annotations

"""
Configuration helpers for server runtime.

This package intentionally keeps logic minimal so modules can import
lightweight helpers without triggering expensive side effects.
"""
