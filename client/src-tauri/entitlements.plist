<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Allow outgoing network connections -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow incoming network connections (for local backend server) -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- Allow loading unsigned executable code (for PyInstaller backend) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (for PyInstaller) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation for bundled binaries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow JIT compilation if needed -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
</dict>
</plist>
